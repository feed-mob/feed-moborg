{
  "name": "feed-moborg-docs-mcp",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:db": "node --experimental-strip-types scripts/build-db.ts",
    "start": "node --experimental-sqlite dist/http.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
